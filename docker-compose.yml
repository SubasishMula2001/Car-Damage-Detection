version: "3.8"
services:
  api:
    build: .
    image: car-damage:latest
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend:ro
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
